name: Publish WFX Smart Stable Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    name: Build and publish Windows release
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements-dev.txt

      - name: Run tests
        run: python -m pytest -q

      - name: Build Windows app
        shell: powershell
        run: |
          python wfx_panel/assets/generate_icon.py
          python -m PyInstaller --noconfirm --clean wfx_panel/wfx-panel.spec

      - name: Package and checksum
        id: package
        shell: powershell
        run: |
          $version = python -c "from wfx_panel.version import APP_VERSION; print(APP_VERSION)"
          $tag = "v$version"
          $zipName = "WFX-Panel-v$version-win64.zip"
          $zipPath = Join-Path $PWD $zipName
          Compress-Archive -Path "dist/WFX-Panel/*" -DestinationPath $zipPath -CompressionLevel Optimal -Force
          $hash = (Get-FileHash -LiteralPath $zipPath -Algorithm SHA256).Hash.ToLowerInvariant()
          [System.IO.File]::WriteAllText(
            "$zipPath.sha256",
            "$hash  $zipName`n",
            [System.Text.UTF8Encoding]::new($false)
          )
          "VERSION=$version" >> $env:GITHUB_ENV
          "TAG=$tag" >> $env:GITHUB_ENV
          "ZIP_NAME=$zipName" >> $env:GITHUB_ENV

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ${{ env.ZIP_NAME }}
            ${{ env.ZIP_NAME }}.sha256
          tag_name: ${{ env.TAG }}
          name: WFX Smart ${{ env.VERSION }}
          body: |
            WFX Smart ${{ env.VERSION }} là bản nâng cấp kiến trúc automation và trải nghiệm launcher/panel dành cho người dùng WFX.

            Cách cài:
            1. Tải file **${{ env.ZIP_NAME }}** bên dưới.
            2. Giải nén toàn bộ thư mục.
            3. Mở **WFX-Panel.exe**.

            Điểm chính:
            - Launcher vuông 48×48 tách thành cửa sổ riêng, luôn nổi và kéo mượt.
            - Click launcher để mở panel ngay cạnh; click ra ngoài tự thu panel về launcher.
            - Panel, launcher và thông báo luôn nằm trọn trong đúng màn hình, kể cả ở bốn góc, màn hình phụ, DPI khác nhau hoặc sau khi tháo màn hình.
            - Tách automation thành các module browser/session/catalog/directory để dễ kiểm thử và bảo trì.
            - Catalog tiếp tục hỗ trợ tìm Code/Buyer Reference, Season và Internal CostSheet Status.
            - Lịch sử tác vụ có thời gian chạy, ảnh lỗi và nút chạy lại; không lưu credential.
            - Mở Chrome automation, dùng lại phiên đăng nhập WFX và chuyển Division WOVEN / KNIT / PSSG.
            - Trình cập nhật một nút giữ nguyên Settings sau khi cài bản mới.

            File .sha256 dùng để kiểm tra gói tải về không bị thay đổi.
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
