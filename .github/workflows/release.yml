name: Publish WFX Smart Stable Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    name: Build, sign and publish Windows release
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Checkout source
        # actions/checkout v5 (Node 24) — pin full SHA để action là immutable.
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          persist-credentials: false

      - name: Set up Python
        # actions/setup-python v6 (Node 24) — pin full SHA.
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements-dev.txt

      - name: Install Inno Setup compiler
        shell: powershell
        run: |
          $compilerCandidates = @(
            (Join-Path $env:LOCALAPPDATA "Programs\Inno Setup 6\ISCC.exe"),
            "C:\Program Files\Inno Setup 6\ISCC.exe",
            "C:\Program Files (x86)\Inno Setup 6\ISCC.exe"
          )
          $compiler = $compilerCandidates |
            Where-Object { Test-Path -LiteralPath $_ } |
            Select-Object -First 1
          if (-not $compiler) {
            winget install `
              --id JRSoftware.InnoSetup `
              --exact `
              --version 6.7.3 `
              --silent `
              --disable-interactivity `
              --accept-package-agreements `
              --accept-source-agreements
            if ($LASTEXITCODE -ne 0) {
              throw "Không cài được Inno Setup compiler."
            }
          }

      - name: Run tests
        run: |
          python -m ruff check .
          python -m pip_audit -r requirements.txt
          python -m pytest -q

      - name: Prepare Windows signing identity
        shell: powershell
        env:
          WFX_SIGNING_CERTIFICATE_BASE64: ${{ secrets.WFX_SIGNING_CERTIFICATE_BASE64 }}
          WFX_SIGNING_CERTIFICATE_PASSWORD: ${{ secrets.WFX_SIGNING_CERTIFICATE_PASSWORD }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:WFX_SIGNING_CERTIFICATE_BASE64)) {
            throw "Thiếu secret WFX_SIGNING_CERTIFICATE_BASE64."
          }
          if ([string]::IsNullOrWhiteSpace($env:WFX_SIGNING_CERTIFICATE_PASSWORD)) {
            throw "Thiếu secret WFX_SIGNING_CERTIFICATE_PASSWORD."
          }
          $certPath = Join-Path $env:RUNNER_TEMP "wfx-signing.pfx"
          [IO.File]::WriteAllBytes(
            $certPath,
            [Convert]::FromBase64String($env:WFX_SIGNING_CERTIFICATE_BASE64)
          )
          $certificate = [Security.Cryptography.X509Certificates.X509Certificate2]::new(
            $certPath,
            $env:WFX_SIGNING_CERTIFICATE_PASSWORD
          )
          $thumbprint = (
            $certificate.Thumbprint -replace '[^A-Fa-f0-9]', ''
          ).ToUpperInvariant()
          if ($thumbprint -notmatch '^[A-F0-9]{40}$') {
            throw "Certificate ký Windows không có thumbprint hợp lệ."
          }
          @"
          """Danh tính certificate được workflow release tạo tự động."""

          EXPECTED_SIGNER_THUMBPRINT = "$thumbprint"
          "@ | Set-Content -LiteralPath "wfx_panel/_signing_identity.py" -Encoding UTF8

      - name: Build Windows app
        shell: powershell
        env:
          WFX_SYNC_READ_KEY: ${{ secrets.WFX_SYNC_READ_KEY }}
        run: |
          if (-not $env:WFX_SYNC_READ_KEY) {
            throw "Thiếu secret WFX_SYNC_READ_KEY."
          }
          [ordered]@{
            read_key = $env:WFX_SYNC_READ_KEY
            read_url = "https://n8n.itx.io.vn/webhook/wfx-sync-latest"
            publish_url = "https://n8n.itx.io.vn/webhook/wfx-sync-publish"
            company_id = "77400"
            division_key = "01"
          } | ConvertTo-Json | Set-Content -LiteralPath "wfx_panel/assets/sync-config.json" -Encoding UTF8
          python wfx_panel/assets/generate_icon.py
          python -m PyInstaller --noconfirm --clean wfx_panel/wfx-panel.spec
          $buildRoot = Join-Path $PWD "dist/WFX-Panel"
          $buildBytes = (
            Get-ChildItem -LiteralPath $buildRoot -Recurse -File |
              Measure-Object -Property Length -Sum
          ).Sum
          $buildSizeMb = [math]::Round($buildBytes / 1MB, 1)
          Write-Host "Packaged size: $buildSizeMb MB"
          if ($buildSizeMb -gt 180) {
            throw "Bản đóng gói vượt giới hạn 180 MB."
          }
          foreach ($unused in @("PyQt5", "PyQt6", "numpy", "cryptography")) {
            if (
              Test-Path -LiteralPath (
                Join-Path $buildRoot "_internal/$unused"
              )
            ) {
              throw "Bản đóng gói còn dependency không dùng: $unused"
            }
          }

      - name: Build Windows installer
        shell: powershell
        run: .\build-installer.ps1 -SkipAppBuild

      - name: Package and create signed checksum
        id: package
        shell: pwsh
        env:
          WFX_SIGNING_CERTIFICATE_PASSWORD: ${{ secrets.WFX_SIGNING_CERTIFICATE_PASSWORD }}
        run: |
          $certPath = Join-Path $env:RUNNER_TEMP "wfx-signing.pfx"
          try {
            $certificate = [Security.Cryptography.X509Certificates.X509Certificate2]::new(
              $certPath,
              $env:WFX_SIGNING_CERTIFICATE_PASSWORD
            )

            $version = python -c "from wfx_panel.version import APP_VERSION; print(APP_VERSION)"
            $tag = "v$version"
            if (
              $env:GITHUB_REF_TYPE -eq "tag" -and
              $env:GITHUB_REF_NAME -ne $tag
            ) {
              throw (
                "Tag $env:GITHUB_REF_NAME không khớp APP_VERSION $version."
              )
            }
            # Prefix mới cố ý không tương thích updater <= 1.0.8. Bản cũ có
            # thể xóa cả thư mục chứa EXE; người dùng nâng cấp 1.0.9 thủ công
            # một lần, rồi các bản sau dùng updater an toàn.
            $zipName = "WFX-Smart-v$version-win64.zip"
            $zipPath = Join-Path $PWD $zipName
            $checksumPath = "$zipPath.sha256"
            $signaturePath = "$checksumPath.p7s"
            $setupName = "WFX-Smart-Setup-v$version.exe"
            $setupPath = Join-Path $PWD "dist/installer/$setupName"
            $setupChecksumPath = "$setupPath.sha256"
            $setupSignaturePath = "$setupChecksumPath.p7s"
            if (-not (Test-Path -LiteralPath $setupPath -PathType Leaf)) {
              throw "Thiếu bộ cài Windows: $setupPath"
            }
            $packageRootName = "WFX-Smart-v$version"
            $packageStage = Join-Path $env:RUNNER_TEMP $packageRootName
            if (Test-Path -LiteralPath $packageStage) {
              Remove-Item -LiteralPath $packageStage -Recurse -Force
            }
            Copy-Item `
              -LiteralPath (Join-Path $PWD "dist/WFX-Panel") `
              -Destination $packageStage `
              -Recurse
            # bsdtar của Windows runner nén cây PyInstaller nhanh và ổn định
            # hơn Compress-Archive rất nhiều. Bọc trong folder theo version để
            # giải nén thủ công không đụng file rời/phiên bản cũ ở Downloads.
            & tar.exe -a -c -f $zipPath `
              -C $env:RUNNER_TEMP $packageRootName
            if ($LASTEXITCODE -ne 0 -or -not (Test-Path -LiteralPath $zipPath)) {
              throw "Không tạo được gói ZIP phát hành."
            }
            $archiveEntries = @(& tar.exe -tf $zipPath)
            if (
              $LASTEXITCODE -ne 0 -or
              $archiveEntries -notcontains "$packageRootName/WFX-Panel.exe" -or
              -not ($archiveEntries | Where-Object {
                $_ -like "$packageRootName/_internal/*"
              })
            ) {
              throw "Gói ZIP thiếu WFX-Panel.exe hoặc _internal."
            }
            $hash = (
              Get-FileHash -LiteralPath $zipPath -Algorithm SHA256
            ).Hash.ToLowerInvariant()
            [IO.File]::WriteAllText(
              $checksumPath,
              "$hash  $zipName`n",
              [Text.UTF8Encoding]::new($false)
            )

            Add-Type -AssemblyName System.Security.Cryptography.Pkcs
            $content = [Security.Cryptography.Pkcs.ContentInfo]::new(
              [IO.File]::ReadAllBytes($checksumPath)
            )
            $cms = [Security.Cryptography.Pkcs.SignedCms]::new($content, $true)
            $signer = [Security.Cryptography.Pkcs.CmsSigner]::new($certificate)
            $signer.IncludeOption = (
              [Security.Cryptography.X509Certificates.X509IncludeOption]::WholeChain
            )
            $cms.ComputeSignature($signer)
            [IO.File]::WriteAllBytes($signaturePath, $cms.Encode())

            $verifyCms = [Security.Cryptography.Pkcs.SignedCms]::new(
              $content,
              $true
            )
            $verifyCms.Decode([IO.File]::ReadAllBytes($signaturePath))
            $verifyCms.CheckSignature($true)
            if ($verifyCms.SignerInfos.Count -ne 1) {
              throw "Chữ ký CMS phải có đúng một publisher."
            }
            $cmsSigner = $verifyCms.SignerInfos[0].Certificate
            $actualCmsSigner = (
              $cmsSigner.Thumbprint -replace '[^A-Fa-f0-9]', ''
            ).ToUpperInvariant()
            $expectedCmsSigner = (
              $certificate.Thumbprint -replace '[^A-Fa-f0-9]', ''
            ).ToUpperInvariant()
            if ($actualCmsSigner -ne $expectedCmsSigner) {
              throw "Chữ ký CMS không đúng publisher certificate."
            }

            $setupHash = (
              Get-FileHash -LiteralPath $setupPath -Algorithm SHA256
            ).Hash.ToLowerInvariant()
            [IO.File]::WriteAllText(
              $setupChecksumPath,
              "$setupHash  $setupName`n",
              [Text.UTF8Encoding]::new($false)
            )
            $setupContent = [Security.Cryptography.Pkcs.ContentInfo]::new(
              [IO.File]::ReadAllBytes($setupChecksumPath)
            )
            $setupCms = [Security.Cryptography.Pkcs.SignedCms]::new(
              $setupContent,
              $true
            )
            $setupSigner = [Security.Cryptography.Pkcs.CmsSigner]::new(
              $certificate
            )
            $setupSigner.IncludeOption = (
              [Security.Cryptography.X509Certificates.X509IncludeOption]::WholeChain
            )
            $setupCms.ComputeSignature($setupSigner)
            [IO.File]::WriteAllBytes(
              $setupSignaturePath,
              $setupCms.Encode()
            )
            $verifySetupCms = [Security.Cryptography.Pkcs.SignedCms]::new(
              $setupContent,
              $true
            )
            $verifySetupCms.Decode(
              [IO.File]::ReadAllBytes($setupSignaturePath)
            )
            $verifySetupCms.CheckSignature($true)
            if ($verifySetupCms.SignerInfos.Count -ne 1) {
              throw "Chữ ký bộ cài phải có đúng một publisher."
            }
            $actualSetupSigner = (
              $verifySetupCms.SignerInfos[0].Certificate.Thumbprint `
                -replace '[^A-Fa-f0-9]', ''
            ).ToUpperInvariant()
            if ($actualSetupSigner -ne $expectedCmsSigner) {
              throw "Chữ ký bộ cài không đúng publisher certificate."
            }

            "VERSION=$version" >> $env:GITHUB_ENV
            "TAG=$tag" >> $env:GITHUB_ENV
            "ZIP_NAME=$zipName" >> $env:GITHUB_ENV
            "SETUP_NAME=$setupName" >> $env:GITHUB_ENV
          }
          finally {
            Remove-Item `
              -LiteralPath $certPath `
              -Force `
              -ErrorAction SilentlyContinue
          }

      - name: Publish GitHub Release
        # softprops/action-gh-release v3.0.0 (Node 24) — pin full SHA vì action
        # nhận contents:write.
        uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda
        with:
          files: |
            dist/installer/${{ env.SETUP_NAME }}
            dist/installer/${{ env.SETUP_NAME }}.sha256
            dist/installer/${{ env.SETUP_NAME }}.sha256.p7s
            ${{ env.ZIP_NAME }}
            ${{ env.ZIP_NAME }}.sha256
            ${{ env.ZIP_NAME }}.sha256.p7s
          tag_name: ${{ env.TAG }}
          name: WFX Smart ${{ env.VERSION }}
          body: |
            WFX Smart ${{ env.VERSION }} sửa hai lỗi Sale ASN khi xuất
            Invoice + Packing List và chọn Factory từ tên FTY đầy đủ.

            Cách dùng được khuyên dùng:
            1. Tải **${{ env.SETUP_NAME }}** và mở file.
            2. Bộ cài tự đóng đúng WFX Smart đang chạy, nâng cấp tại chỗ và
               tạo shortcut ngoài Desktop.
            3. Tài khoản, Settings và dữ liệu làm việc được giữ nguyên.

            Bản portable vẫn có sẵn: tải **${{ env.ZIP_NAME }}**, giải nén vào
            một thư mục riêng rồi mở **WFX-Panel.exe**.

            **Lưu ý khi nâng cấp từ 1.0.8 trở xuống:** hãy cài thủ công một lần
            theo ba bước trên. Gói mới cố ý không kích hoạt updater cũ để tránh
            nguy cơ updater đó xóa nhầm thư mục chứa ứng dụng.

            **Nếu bản 1.0.9–1.0.11 báo lỗi CMS hoặc hash:** hãy cài thủ công
            1.0.12 một lần. Từ 1.0.12 trở đi, updater tương thích cả Windows
            PowerShell 5.1 và PowerShell 7, đồng thời vẫn xác minh chữ ký, giới
            hạn file được thay và có rollback.

            Điểm chính:
            - Xuất Invoice + PKL tiếp tục xử lý file native khi Chrome không phát sự kiện download qua CDP.
            - Buyer Invoice và Packing List được tải đủ rồi ghép, wrap text và gộp số liệu theo buyer như thiết kế.
            - Factory không còn chọn sớm tên chung như PRO SPORTS khi FTY yêu cầu PRO SPORTS GIAO THUY JSC.
            - Danh sách Factory lazy-bind được chờ đến khi có lựa chọn đầy đủ trước khi điền.

            File `.sha256` và chữ ký `.p7s` xác minh gói tải về đúng từ nhà phát hành.
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
