# Hướng dẫn cho AI — WFX Smart

Nguồn chuẩn (single source of truth) cho đặc tả desktop app WFX Smart và quy
tắc automation nằm ở **[`CLAUDE.md`](./CLAUDE.md)**.

Trước đây `AGENTS.md` là bản sao đầy đủ của `CLAUDE.md`; hai file đã bị lệch nhau
(ví dụ phần hotkey: bản cũ ghi `Ctrl+Alt+X`, bản chuẩn là `Ctrl+Shift+X`). Để
tránh drift, mọi nội dung chi tiết chỉ còn ở `CLAUDE.md`. Hãy đọc file đó.
